- name: Install qemu-guest-agent
  hosts: all
  become: true
  tasks:
    - name: apt
      apt:
        update_cache: yes
        upgrade: 'yes'
    - name: install qemu-guest-agent
      apt:
        name: qemu-guest-agent
        state: present
        update_cache: true