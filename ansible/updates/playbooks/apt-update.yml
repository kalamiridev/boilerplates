- name: Update and check if reboot is required  Debian/Ubuntu
  hosts: all
  become: true
  tasks:
    - name: Update repositories cache
      apt:
        update_cache: yes

    - name: Clean and remove unused dependency packages
      apt:
        autoclean: true
        autoremove: true

    - name: Upgrade all packages
      apt:
        upgrade: "yes"

    - name: Check if reboot is required
      become: true
      stat: path=/var/run/reboot-required get_md5=no
      register: reboot_required

    - name: Test
      telegram:
        token: "{{ telegram_token }}"
        api_args:
          chat_id: "{{ telegram_chat_id }}"
          parse_mode: "html"
          #text: "Reboot: {{ hostvars[inventory_hostname]['ansible_default_ipv4']['address'] }}"
          text: "{{ hostvars[inventory_hostname]['ansible_nodename'] }}"

    - name: Reboot is required message
      telegram:
        token: "{{ telegram_token }}"
        api_args:
          chat_id: "{{ telegram_chat_id }}"
          parse_mode: "html"
          text: Reboot
      when: reboot_required.stat.exists
