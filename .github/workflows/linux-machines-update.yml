name: Linux Machines Update

on: 
    push:
        branches:
            - main

jobs:
    deploy:
        runs-on: "self-hosted"
        steps:
            - name: Checkout
              uses: actions/checkout@2
        