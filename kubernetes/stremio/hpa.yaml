apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: stremio-hpa
  namespace: media
  labels:
    app: stremio
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: stremio
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 90