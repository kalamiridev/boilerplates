apiVersion: apps/v1
kind: Deployment
metadata:
  name: stremio
  namespace: media
  labels:
    app: stremio
spec:
  replicas: 2
  selector:
    matchLabels:
      app: stremio
  template:
    metadata:
      labels:
        app: stremio
    spec:
      containers:
      - name: stremio
        image: stremio/server:latest
        ports:
        - containerPort: 11470
        resources:
          requests:
            memory: "20Mi"
            cpu: "500m"
          limits:
            memory: "1000Mi"
            cpu: "2000m"
        volumeMounts:
            - mountPath: config
              name: docker
              subPath: stremio
            - mountPath: root/.stremio-server
              name: docker
              subPath: stremio
      volumes:
        - hostPath:
            path: /docker
            type: Directory
          name: docker
      