services:
  redis:
    image: redis/redis-stack:latest
    container_name: redis
    restart: unless-stopped
    ports:
      - "6379:6379"
      - "8001:8001"
    security_opt:
      - apparmor:unconfined
    volumes:
      - data:/data

volumes:
  data:
    name: redis_data
