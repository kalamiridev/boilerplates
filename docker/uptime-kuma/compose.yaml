services:
  uptime-kuma:
    image: louislam/uptime-kuma:2
    container_name: uptime-kuma
    restart: unless-stopped
    ports:
      - "3444:3001"
    volumes:
      - data:/app/data

volumes:
  data:
    name: uptime-kuma_data
